# 视野

Zero Draft 存在，是为了让一个人写完草稿之前的那篇短草稿。
它服务坐在键盘前的那一个人。
它把限时只许向前的写作，变成留下的一段，或者一次留得住的失去。
它只拥有一件事：计时、只许追加的写作会话，以及让这件事成立的威胁。
网页产品叫 Zero Draft；原生 Mac 应用叫 Write It Down。First Line 不再对外使用。

## 机制就是契约

网页会话六十秒，沉默五秒警告、八秒清空。
原生 Mac 会话在开始前选一、五、十、二十或三十分钟，停手清空选五、八或十二秒，警告始终落在清空前最后三秒。
节拍是一百毫秒。
这些数字是平台各自的契约，不能为了图方便随意改动。
输入只许向前：插入文字、换段、换行，或者正在组成的输入。
退格、删除、粘贴、剪切、拖放、撤销，以及改写过去的光标动作，一律拒绝。
到点那一刻没结果，会话按失败算。

## 威胁必须有牙

删除不是堆肥，不是安慰，也不是闪两秒就没了。
失败的会话失去当前这一段，并留下痕迹。
没动过的会话不会进入危险，也不会算成功。
仁慈只存在于第一个字之前。
第一个字之后，威胁说了算。
失败卡片不卖东西。

## 组字

未完成的输入法组字算活动，不算沉默。
守卫不能在标记文本期间拒绝、重建，或把光标弹走。
非 BMP 字符留在 UTF-16 空间里。
一次恢复、焦点修复或换皮如果削弱这些守卫，就是缺陷。

## 换皮不能动引擎

`design/DESIGN.md` 是视觉宪法。
新页面先复用它的令牌、化石规则和危险编排，再发明变体。
红色不是装饰。
蓝色不用。
代码和文案里不出现长破折号和短破折号。
换皮如果碰到会话信号、事件桥或只许向前的守卫，就拒绝。
网页状态只活在信号里，JavaScript 是无状态的桥，绝不当第二个写入者。
落地页没有 localStorage。

## 两个平台

网页落地是一个 Datastar 原生文件。
Mac 应用是纯 AppKit。
两个平台不共享运行时、SwiftUI 垫片或 WebView。
当前代码和测试压过历史 PRD、计划和原型。
死代码删掉。
写下来的东西和跑起来的东西要对得上，对不上，这改动就没做完。

## 范围

它不是温柔的写作陪伴。
它不是 750 Words，不是连续打卡，不是账号，也不是分析产品。
里面没有人工智能。
写作不离开这台机器。
不加引导页、侧栏或第二块写作面。
商业可以是安静、收住的许可，不搭失败那一刻的便车，也不长成 SaaS 外壳。
历史设计演示是档案，不是待办。
这份文件是验收政策，管改动能不能合。
它不压过 `design/DESIGN.md`（视觉宪法），也不压过 AGENTS.md（流程）。
围城运动的合同在仓库的 siege-motion-model.md 里，不在私人 .pi 目录里。

## 对齐与抵制

一项改动合不合格，看它守不守得住这几件事：时间契约、只许向前、不软化的清除。
让输入法更安全的改动，对齐。
把皮和引擎分开的改动，对齐。
删掉死路并且证明守卫还在的改动，对齐。
让两个平台更纯的改动，对齐。
软化删除的改动，抵制。
加一条回头路的改动，抵制。
把写作存到页外的改动，抵制。
混用两套运行时的改动，抵制。
拿红当装饰的改动，抵制。
藏起失去时必须看见的确认的改动，抵制。
发明新的写作面的改动，抵制。
把历史计划当成现行法律的改动，抵制。
